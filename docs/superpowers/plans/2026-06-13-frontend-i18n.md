# 前端 i18n 多语言切换 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 为 frontend 加入中英双语切换，全部页面与组件的可见静态 UI 文案改为 `t()` 驱动，默认中文，选择持久化。

**Architecture:** 轻量自建方案——React Context + 强类型字典（`zh`/`en`）+ 泛型 `t(path, vars)`。`translator.ts` 是无 React 依赖的纯函数（可独立单测）；`context.tsx` 在其上提供 `useI18n()`。`t()` 的 key 受 `NestedKeyOf<Translation>` 约束，拼错编译失败。语言偏好存 `localStorage("vibe-lang")`，默认 `zh`。

**Tech Stack:** React 19 + Vite 6 + TypeScript(strict) + Zustand + react-router-dom 7 + Tailwind 3；测试 vitest(globals, jsdom) + @testing-library/react。

**Spec:** `docs/superpowers/specs/2026-06-13-frontend-i18n-design.md`

---

## 全局说明

### Commit 策略
用户已表明本工作暂不提交 git 的偏好。每个 Task 末尾的 `git commit` 步骤为**可选检查点**——需要时执行 `git commit -s`（项目要求 DCO `Signed-off-by` 尾注），不需要则跳过。所有 `npm run build` 与 `npx vitest run` 验证步骤**无论是否提交都必须执行**。

### 验证命令（贯穿全程）
所有命令在 `frontend/` 下执行：
- 类型 + 构建：`npm run build`（即 `tsc -b && vite build`）
- 单测：`npx vitest run`
- 单文件单测：`npx vitest run src/i18n/__tests__/<file>.test.ts`

### 迁移通用方法（适用于 Task 6 起的所有迁移任务）

对每个硬编码的**可见**英文字符串，执行三步：

1. **命名 key**：按 `namespace.语义名` 命名（camelCase），复用词归 `common`，页面/组件特有词归各自 namespace。
2. **加字典**：在 `src/i18n/locales/zh.ts` 与 `en.ts` 的对应 namespace 下新增该 key（中文译值 / 英文原值）。
3. **替换**：把字符串改为 `t("namespace.key")`；含动态部分用插值 `t("namespace.key", { var })`，字典值里写 `{{var}}`。

**Before/After 示例（真实代码）：**

*示例 A — 数组里的 label 字段（`Layout.tsx` 的 NAV）：*
```tsx
// before
const NAV = [
  { to: "/", icon: BarChart3, label: "Home" },
  { to: "/agent", icon: Bot, label: "Agent" },
  { to: "/alpha-zoo", icon: Layers, label: "Alpha Zoo" },
  { to: "/settings", icon: Settings, label: "Settings" },
  { to: "/correlation", icon: BarChart3, label: "Correlation Matrix" },
];
// after（label 改为 key，渲染处用 t）
const NAV = [
  { to: "/", icon: BarChart3, labelKey: "nav.home" as const },
  { to: "/agent", icon: Bot, labelKey: "nav.agent" as const },
  { to: "/alpha-zoo", icon: Layers, labelKey: "nav.alphaZoo" as const },
  { to: "/settings", icon: Settings, labelKey: "nav.settings" as const },
  { to: "/correlation", icon: BarChart3, labelKey: "nav.correlation" as const },
];
// 渲染处：const { t } = useI18n(); ... {!collapsed && t(labelKey)}
```

*示例 B — JSX 文本与 title 属性（`Layout.tsx` footer）：*
```tsx
// before
<button title="Expand"><ChevronsRight /></button>
<p>No sessions yet</p>
// after
<button title={t("common.expand")}><ChevronsRight /></button>
<p>{t("layout.noSessions")}</p>
```

*示例 C — 插值（`WelcomeScreen.tsx` 示例描述）：*
```tsx
// before
desc: "Parse 同花顺/东财/富途/generic CSV — holding days, win rate, PnL ratio, hourly distribution",
// after —— zh.ts: chat: { sampleDesc: "解析 {{sources}} CSV — 持有天数、胜率、盈亏比、小时分布" }
//          en.ts: chat: { sampleDesc: "Parse {{sources}} CSV — holding days, win rate, PnL ratio, hourly distribution" }
t("chat.sampleDesc", { sources: "同花顺/东财/富途" }) // zh 渲染: 解析 同花顺/东财/富途 CSV — …
```

*示例 D — Stat label 字段（`ValidationPanel.tsx`）：*
```tsx
// before
<Stat label="Actual Sharpe" value={mc.actual_sharpe.toFixed(2)} />
<th className="...">Period</th>
// after
<Stat label={t("charts.validation.actualSharpe")} value={mc.actual_sharpe.toFixed(2)} />
<th className="...">{t("charts.validation.period")}</th>
```

*示例 E — 空状态/条件文案：*
```tsx
// before
return <p>No validation data available.</p>;
// after
return <p>{t("charts.validation.noData")}</p>;
```

**迁移纪律**：
- 只迁移**面向用户的可见字符串**（JSX 文本、`title`/`placeholder`/`aria-label`/按钮文字/表头/标签/空状态/错误提示）。
- **不迁移**：className、import 路径、事件名、纯数据 key（如 `session_id`）、技术性 prop 值（颜色、类名）、API 字段名。
- **不翻译**：agent 返回的动态内容、股票代码（`000001.SZ`/`BTC-USDT`/`AAPL`）、专业符号。
- 每个文件迁移后立即 `npm run build`，确保类型与引用正确（`t()` 的 key 拼错会在 `tsc -b` 报错）。

---

## File Structure

| 文件 | 职责 | 创建/修改 |
| --- | --- | --- |
| `src/i18n/types.ts` | `Lang`、`NestedKeyOf<T>`、`KeyPath`、`Vars`、`Translator<T>` 类型与常量 | 创建 |
| `src/i18n/translator.ts` | 纯函数 `get`/`interpolate`/`createTranslator`（无 React 依赖） | 创建 |
| `src/i18n/locales/zh.ts` | 简中源字典（`as const`），导出 `Translation` 类型 | 创建 |
| `src/i18n/locales/en.ts` | 英文字典，`satisfies Translation` | 创建 |
| `src/i18n/context.tsx` | `LanguageProvider` + `useI18n()`（React 绑定 + 检测/持久化） | 创建 |
| `src/i18n/index.ts` | 对外出口 | 创建 |
| `src/i18n/__tests__/translator.test.ts` | 纯函数单测 | 创建 |
| `src/i18n/__tests__/locales.test.ts` | zh/en key 集合一致性单测 | 创建 |
| `src/i18n/__tests__/context.test.tsx` | Provider/useI18n 单测 | 创建 |
| `src/main.tsx` | 挂载 `<LanguageProvider>` | 修改 |
| `src/components/layout/Layout.tsx` | sidebar footer 加切换器；文案迁移 | 修改 |
| `src/router.tsx` 及 `pages/*`、`components/{chat,charts,common,layout}/*` | 文案迁移 | 修改 |

---

## Task 1: 类型层 `types.ts`

**Files:**
- Create: `src/i18n/types.ts`

- [ ] **Step 1: 创建 `src/i18n/types.ts`**

```ts
export type Lang = "zh" | "en";

export const LANGS: readonly Lang[] = ["zh", "en"] as const;
export const DEFAULT_LANG: Lang = "zh";
export const LANG_STORAGE_KEY = "vibe-lang";

/**
 * Recursively build a dot-notation union of leaf string paths in T.
 * { a: { b: "x" } } => "a.b"
 */
export type NestedKeyOf<T> = T extends object
  ? {
      [K in keyof T & string]: T[K] extends string
        ? `${K}`
        : T[K] extends object
          ? `${K}.${NestedKeyOf<T[K]>}`
          : never;
    }[keyof T & string]
  : never;

/** Variables used for {{name}} interpolation. */
export type Vars = Record<string, string | number>;
```

- [ ] **Step 2: 验证类型推导（编译检查）**

Run: `cd frontend && npx tsc -b`
Expected: PASS（无错误）。此步只是建立文件，引用尚未发生，不会报错。

- [ ] **Step 3: (可选) Commit**

```bash
git add src/i18n/types.ts && git commit -s -m "feat(i18n): add type primitives (Lang, NestedKeyOf, Vars)"
```

---

## Task 2: 翻译器纯函数 `translator.ts`（TDD 核心）

**Files:**
- Create: `src/i18n/translator.ts`
- Test: `src/i18n/__tests__/translator.test.ts`

- [ ] **Step 1: 写失败测试 `src/i18n/__tests__/translator.test.ts`**

```ts
import { get, interpolate, createTranslator } from "../translator";

describe("get", () => {
  const obj = { a: { b: { c: "leaf" }, n: 5 }, x: "top" };
  it("reads nested leaf string", () => {
    expect(get(obj, "a.b.c")).toBe("leaf");
  });
  it("reads top-level value", () => {
    expect(get(obj, "x")).toBe("top");
  });
  it("returns undefined for missing path", () => {
    expect(get(obj, "a.b.z")).toBeUndefined();
  });
  it("returns undefined when intermediate is not an object", () => {
    expect(get(obj, "x.y")).toBeUndefined();
  });
});

describe("interpolate", () => {
  it("replaces {{name}} with value", () => {
    expect(interpolate("hello {{name}}", { name: "world" })).toBe("hello world");
  });
  it("handles multiple placeholders and numbers", () => {
    expect(interpolate("{{a}}/{{b}}", { a: 1, b: 2 })).toBe("1/2");
  });
  it("leaves unknown placeholders intact when no vars", () => {
    expect(interpolate("hi {{name}}")).toBe("hi {{name}}");
  });
  it("leaves unknown placeholders intact when var missing", () => {
    expect(interpolate("hi {{name}}", { other: "x" })).toBe("hi {{name}}");
  });
});

describe("createTranslator", () => {
  const dict = { greet: "hi {{name}}", bye: "bye", nested: { ok: "ok" } };
  const { t } = createTranslator(dict);

  it("translates a leaf key", () => {
    expect(t("bye")).toBe("bye");
  });
  it("translates a nested key", () => {
    expect(t("nested.ok")).toBe("ok");
  });
  it("interpolates vars", () => {
    expect(t("greet", { name: "ann" })).toBe("hi ann");
  });
  it("returns the path itself for missing key", () => {
    expect(t("nope" as never)).toBe("nope");
  });
  it("warns in dev for missing key", () => {
    const spy = vi.spyOn(console, "warn").mockImplementation(() => {});
    t("missing" as never);
    expect(spy).toHaveBeenCalledWith(expect.stringContaining("missing translation key"));
    spy.mockRestore();
  });
});
```

- [ ] **Step 2: 运行测试，确认失败**

Run: `cd frontend && npx vitest run src/i18n/__tests__/translator.test.ts`
Expected: FAIL（`Cannot find module '../translator'`）。

- [ ] **Step 3: 实现 `src/i18n/translator.ts`**

```ts
import type { NestedKeyOf, Vars } from "./types";

/** Read a dot-path value from an object. Returns undefined on any missing/non-object segment. */
export function get(obj: unknown, path: string): unknown {
  const parts = path.split(".");
  let cur: unknown = obj;
  for (const p of parts) {
    if (cur && typeof cur === "object" && p in (cur as Record<string, unknown>)) {
      cur = (cur as Record<string, unknown>)[p];
    } else {
      return undefined;
    }
  }
  return cur;
}

/** Replace {{name}} placeholders in `s` using `vars`. Unknown placeholders are left intact. */
export function interpolate(s: string, vars?: Vars): string {
  if (!vars) return s;
  return s.replace(/\{\{\s*(\w+)\s*\}\}/g, (m, key: string) =>
    key in vars ? String(vars[key]) : m,
  );
}

export interface Translator<T> {
  t: (path: NestedKeyOf<T>, vars?: Vars) => string;
}

/**
 * Build a `t(path, vars?)` bound to `dict`. Missing/non-string keys return the
 * path itself; in dev mode a warning is logged. Pure — no React dependency.
 */
export function createTranslator<T extends object>(dict: T): Translator<T> {
  const t = (path: NestedKeyOf<T>, vars?: Vars): string => {
    const val = get(dict, path as string);
    if (typeof val !== "string") {
      if (import.meta.env.DEV) {
        console.warn(`[i18n] missing translation key: "${path}"`);
      }
      return path as string;
    }
    const out = interpolate(val, vars);
    if (import.meta.env.DEV && /\{\{\s*\w+\s*\}\}/.test(out)) {
      console.warn(`[i18n] unresolved placeholder in "${path}": ${out}`);
    }
    return out;
  };
  return { t };
}
```

- [ ] **Step 4: 运行测试，确认通过**

Run: `cd frontend && npx vitest run src/i18n/__tests__/translator.test.ts`
Expected: PASS（全部用例绿）。

- [ ] **Step 5: (可选) Commit**

```bash
git add src/i18n/translator.ts src/i18n/__tests__/translator.test.ts
git commit -s -m "feat(i18n): add pure translator (get/interpolate/createTranslator) with tests"
```

---

## Task 3: 基础字典 `locales/zh.ts` + `locales/en.ts` + 一致性测试

本任务只落地框架层所需的 namespace（`common`/`nav`/`layout`/`router`/`connection`/`errorBoundary`）。其余 namespace 由 Task 6+ 迁移时追加进同一文件。

**Files:**
- Create: `src/i18n/locales/zh.ts`
- Create: `src/i18n/locales/en.ts`
- Test: `src/i18n/__tests__/locales.test.ts`

- [ ] **Step 1: 创建源字典 `src/i18n/locales/zh.ts`**

```ts
export const zh = {
  common: {
    confirm: "确认",
    cancel: "取消",
    save: "保存",
    delete: "删除",
    loading: "加载中…",
    rename: "重命名",
    expand: "展开",
    collapse: "收起",
    search: "搜索",
    refresh: "刷新",
    copy: "复制",
    copied: "已复制",
    close: "关闭",
    error: "出错了",
    retry: "重试",
    back: "返回",
  },
  nav: {
    home: "首页",
    agent: "Agent",
    alphaZoo: "Alpha 动物园",
    settings: "设置",
    correlation: "相关性矩阵",
  },
  layout: {
    sessions: "会话",
    newChat: "新对话",
    noSessions: "暂无会话",
    light: "浅色",
    dark: "深色",
    switchToZh: "中文",
    switchToEn: "English",
  },
  router: {
    loading: "加载中…",
  },
  connection: {
    lost: "连接已断开",
    reconnecting: "正在重连…",
  },
  errorBoundary: {
    title: "出错了",
    reload: "重新加载",
  },
} as const;

export type Translation = typeof zh;
```

> `connection`/`errorBoundary` 的 key 以现有源文件实际文案为准；若 `ConnectionBanner.tsx`/`ErrorBoundary.tsx` 中还有其它可见文案，在 Task 6 读取文件后在此 namespace 内补齐 key。

- [ ] **Step 2: 创建英文字典 `src/i18n/locales/en.ts`**

```ts
import type { Translation } from "./zh";

export const en: Translation = {
  common: {
    confirm: "Confirm",
    cancel: "Cancel",
    save: "Save",
    delete: "Delete",
    loading: "Loading…",
    rename: "Rename",
    expand: "Expand",
    collapse: "Collapse",
    search: "Search",
    refresh: "Refresh",
    copy: "Copy",
    copied: "Copied",
    close: "Close",
    error: "Error",
    retry: "Retry",
    back: "Back",
  },
  nav: {
    home: "Home",
    agent: "Agent",
    alphaZoo: "Alpha Zoo",
    settings: "Settings",
    correlation: "Correlation Matrix",
  },
  layout: {
    sessions: "Sessions",
    newChat: "New Chat",
    noSessions: "No sessions yet",
    light: "Light",
    dark: "Dark",
    switchToZh: "中文",
    switchToEn: "English",
  },
  router: {
    loading: "Loading…",
  },
  connection: {
    lost: "Connection lost",
    reconnecting: "Reconnecting…",
  },
  errorBoundary: {
    title: "Something went wrong",
    reload: "Reload",
  },
};
```

- [ ] **Step 3: 写一致性测试 `src/i18n/__tests__/locales.test.ts`**

```ts
import { zh } from "../locales/zh";
import { en } from "../locales/en";

/** Collect every dot-path of string leaves in a nested dict. */
function leafPaths(obj: unknown, prefix = ""): string[] {
  if (obj && typeof obj === "object") {
    return Object.entries(obj as Record<string, unknown>).flatMap(([k, v]) =>
      leafPaths(v, prefix ? `${prefix}.${k}` : k),
    );
  }
  return prefix ? [prefix] : [];
}

function walk(a: unknown, b: unknown, path: string): string[] {
  const diffs: string[] = [];
  if (b && typeof b === "object") {
    if (!a || typeof a !== "object") diffs.push(path);
    else
      for (const k of Object.keys(b as object))
        diffs.push(...walk((a as Record<string, unknown>)[k], (b as Record<string, unknown>)[k], path ? `${path}.${k}` : k));
  }
  return diffs;
}

describe("locales", () => {
  it("zh has only string leaves", () => {
    for (const p of leafPaths(zh)) {
      const val = p.split(".").reduce<unknown>((o, k) => (o as Record<string, unknown>)[k], zh);
      expect(typeof val).toBe("string");
    }
  });

  it("en has exactly the same key set as zh", () => {
    const missing = walk(en, zh, "");
    expect(missing, `en is missing keys: ${missing.join(", ")}`).toEqual([]);
    const extra = walk(zh, en, "");
    expect(extra, `en has extra keys: ${extra.join(", ")}`).toEqual([]);
  });

  it("no empty translation values", () => {
    for (const p of leafPaths(zh)) {
      const v = (p.split(".").reduce<unknown>((o, k) => (o as Record<string, unknown>)[k], zh) as string).trim();
      expect(v.length).toBeGreaterThan(0);
    }
    for (const p of leafPaths(en)) {
      const v = (p.split(".").reduce<unknown>((o, k) => (o as Record<string, unknown>)[k], en) as string).trim();
      expect(v.length).toBeGreaterThan(0);
    }
  });
});
```

- [ ] **Step 4: 运行测试，确认通过**

Run: `cd frontend && npx vitest run src/i18n/__tests__/locales.test.ts`
Expected: PASS。

- [ ] **Step 5: (可选) Commit**

```bash
git add src/i18n/locales src/i18n/__tests__/locales.test.ts
git commit -s -m "feat(i18n): add zh/en base dictionaries + key-parity tests"
```

---

## Task 4: Context 与出口 `context.tsx` + `index.ts`（TDD）

**Files:**
- Create: `src/i18n/context.tsx`
- Create: `src/i18n/index.ts`
- Test: `src/i18n/__tests__/context.test.tsx`

- [ ] **Step 1: 写失败测试 `src/i18n/__tests__/context.test.tsx`**

```tsx
import { render, screen, act } from "@testing-library/react";
import { LanguageProvider, useI18n } from "../context";

function Probe() {
  const { t, lang, setLang } = useI18n();
  return (
    <>
      <span data-testid="val">{t("nav.home")}</span>
      <span data-testid="lang">{lang}</span>
      <button onClick={() => setLang("en")}>switch</button>
    </>
  );
}

describe("LanguageProvider", () => {
  beforeEach(() => {
    localStorage.clear();
    document.documentElement.lang = "";
  });

  it("defaults to zh", () => {
    render(<LanguageProvider><Probe /></LanguageProvider>);
    expect(screen.getByTestId("lang").textContent).toBe("zh");
    expect(screen.getByTestId("val").textContent).toBe("首页");
  });

  it("initializes from localStorage", () => {
    localStorage.setItem("vibe-lang", "en");
    render(<LanguageProvider><Probe /></LanguageProvider>);
    expect(screen.getByTestId("lang").textContent).toBe("en");
    expect(screen.getByTestId("val").textContent).toBe("Home");
  });

  it("falls back to zh for invalid stored value", () => {
    localStorage.setItem("vibe-lang", "fr");
    render(<LanguageProvider><Probe /></LanguageProvider>);
    expect(screen.getByTestId("lang").textContent).toBe("zh");
  });

  it("setLang persists and syncs <html lang>", () => {
    render(<LanguageProvider><Probe /></LanguageProvider>);
    act(() => {
      screen.getByText("switch").click();
    });
    expect(screen.getByTestId("lang").textContent).toBe("en");
    expect(screen.getByTestId("val").textContent).toBe("Home");
    expect(localStorage.getItem("vibe-lang")).toBe("en");
    expect(document.documentElement.lang).toBe("en");
  });

  it("throws when used outside Provider", () => {
    const err = vi.spyOn(console, "error").mockImplementation(() => {});
    expect(() => render(<Probe />)).toThrow(/useI18n.*Provider/i);
    err.mockRestore();
  });
});
```

- [ ] **Step 2: 运行测试，确认失败**

Run: `cd frontend && npx vitest run src/i18n/__tests__/context.test.tsx`
Expected: FAIL（`Cannot find module '../context'`）。

- [ ] **Step 3: 实现 `src/i18n/context.tsx`**

```tsx
import { createContext, useContext, useMemo, useState, type ReactNode } from "react";
import { createTranslator, type Translator } from "./translator";
import { zh } from "./locales/zh";
import { en } from "./locales/en";
import { DEFAULT_LANG, LANG_STORAGE_KEY, LANGS, type Lang, type NestedKeyOf, type Vars } from "./types";
import type { Translation } from "./locales/zh";

type Dictionaries = Record<Lang, Translation>;
const DICTS: Dictionaries = { zh, en };

function detectLang(): Lang {
  try {
    const stored = localStorage.getItem(LANG_STORAGE_KEY);
    if (stored && (LANGS as readonly string[]).includes(stored)) return stored as Lang;
  } catch {
    /* localStorage unavailable — fall through to default */
  }
  return DEFAULT_LANG;
}

interface I18nValue extends Translator<Translation> {
  /** Loose translator for dynamically-built keys (e.g. metric labels). Bypasses compile-time key check. */
  tRaw: (path: string, vars?: Vars) => string;
  lang: Lang;
  setLang: (lang: Lang) => void;
}

const I18nContext = createContext<I18nValue | null>(null);

export function LanguageProvider({ children }: { children: ReactNode }) {
  const [lang, setLangState] = useState<Lang>(detectLang);

  const value = useMemo<I18nValue>(() => {
    const { t } = createTranslator<Translation>(DICTS[lang]);
    const tRaw = (path: string, vars?: Vars): string => t(path as NestedKeyOf<Translation>, vars);
    const setLang = (next: Lang) => {
      setLangState(next);
      try {
        localStorage.setItem(LANG_STORAGE_KEY, next);
      } catch {
        /* ignore persistence errors */
      }
      document.documentElement.lang = next;
    };
    return { t, tRaw, lang, setLang };
  }, [lang]);

  return <I18nContext.Provider value={value}>{children}</I18nContext.Provider>;
}

export function useI18n(): I18nValue {
  const ctx = useContext(I18nContext);
  if (!ctx) throw new Error("useI18n must be used within a LanguageProvider");
  return ctx;
}
```

> 注意：`detectLang` 在 `useState(detectLang)` 中作为惰性初始化函数传入（仅首渲染执行一次）。`setLang` 首次调用后才同步 `<html lang>`；为使首渲染也有正确 `lang` 属性，在 Task 5 挂载后可由 Provider effect 补设——见 Step 4 的微调。

- [ ] **Step 3b: 为 `<html lang>` 首渲染正确，给 Provider 加 effect**

在 `LanguageProvider` 内 `useState` 之后、`useMemo` 之前补：

```tsx
  useEffect(() => {
    document.documentElement.lang = lang;
  }, [lang]);
```

并在文件顶部 import 增加 `useEffect`：
```tsx
import { createContext, useContext, useEffect, useMemo, useState, type ReactNode } from "react";
```

- [ ] **Step 4: 运行测试，确认通过**

Run: `cd frontend && npx vitest run src/i18n/__tests__/context.test.tsx`
Expected: PASS（全部用例绿，含 `<html lang>` 同步与 Provider 外抛错）。

- [ ] **Step 5: 创建出口 `src/i18n/index.ts`**

```ts
export { LanguageProvider, useI18n } from "./context";
export type { Lang } from "./types";
export type { Translation } from "./locales/zh";
```

- [ ] **Step 6: 全量构建确认无类型错误**

Run: `cd frontend && npm run build`
Expected: PASS（`tsc -b && vite build` 全绿）。

- [ ] **Step 7: (可选) Commit**

```bash
git add src/i18n/context.tsx src/i18n/index.ts src/i18n/__tests__/context.test.tsx
git commit -s -m "feat(i18n): add LanguageProvider/useI18n with detection + persistence"
```

---

## Task 5: 挂载 Provider（`main.tsx`）

**Files:**
- Modify: `src/main.tsx`

- [ ] **Step 1: 在 `main.tsx` 包裹 `<LanguageProvider>`**

把现有：
```tsx
import { ErrorBoundary } from "./components/common/ErrorBoundary";
import { router } from "./router";
```
改为在其后新增 import：
```tsx
import { LanguageProvider } from "./i18n";
```
并把 `render` 内容从：
```tsx
  <StrictMode>
    <ErrorBoundary>
      <RouterProvider router={router} />
      <Toaster position="bottom-right" richColors closeButton duration={3500} />
    </ErrorBoundary>
  </StrictMode>
```
改为：
```tsx
  <StrictMode>
    <LanguageProvider>
      <ErrorBoundary>
        <RouterProvider router={router} />
        <Toaster position="bottom-right" richColors closeButton duration={3500} />
      </ErrorBoundary>
    </LanguageProvider>
  </StrictMode>
```

> `LanguageProvider` 在最外层，确保 `ErrorBoundary`/`Toaster`/路由内所有组件都能用 `useI18n()`。

- [ ] **Step 2: 构建确认**

Run: `cd frontend && npm run build`
Expected: PASS。

- [ ] **Step 3: (可选) Commit**

```bash
git add src/main.tsx && git commit -s -m "feat(i18n): mount LanguageProvider at app root"
```

---

## Task 6: 语言切换器 UI（`Layout.tsx` sidebar footer）

**Files:**
- Modify: `src/components/layout/Layout.tsx`

- [ ] **Step 1: 在 `Layout.tsx` 引入 `useI18n` 并加切换按钮**

文件顶部 import 区增加：
```tsx
import { useI18n } from "@/i18n";
import { Languages } from "lucide-react";
```
在组件函数体 `const { dark, toggle } = useDarkMode();` 下一行增加：
```tsx
  const { lang, setLang } = useI18n();
```

- [ ] **Step 2: 在 sidebar footer 两个分支各加一个切换按钮**

**收起态分支**（`{collapsed ? ( <> ... </> )` 内，紧挨 dark mode `<button onClick={toggle}>` 之后）插入：
```tsx
              <button
                onClick={() => setLang(lang === "zh" ? "en" : "zh")}
                className="p-1.5 text-muted-foreground hover:text-foreground rounded transition-colors"
                title={lang === "zh" ? t("layout.switchToEn") : t("layout.switchToZh")}
              >
                <span className="text-xs font-medium">{lang === "zh" ? "EN" : "中"}</span>
              </button>
```

**展开态分支**（`:` 后的 `<>` 内，dark mode 按钮那一行 `<div className="flex items-center justify-between">` 中，在 collapse 按钮所在的右侧 `<div className="flex items-center gap-1">` 内，`ChevronsLeft` 按钮之前）插入：
```tsx
                    <button
                      onClick={() => setLang(lang === "zh" ? "en" : "zh")}
                      className="p-1 text-muted-foreground hover:text-foreground rounded transition-colors"
                      title={lang === "zh" ? t("layout.switchToEn") : t("layout.switchToZh")}
                    >
                      <Languages className="h-3.5 w-3.5" />
                    </button>
```

> 注：本任务只接入切换器；`Layout` 内其余文案迁移在 Task 7 一起做。本步需要 `useI18n` 返回的 `t`——在函数体已解构（本 Task Step 1 已加 `lang/setLang`，再加 `t`：把那行改为 `const { t, lang, setLang } = useI18n();`）。

- [ ] **Step 3: 构建确认**

Run: `cd frontend && npm run build`
Expected: PASS。

- [ ] **Step 4: (可选) Commit**

```bash
git add src/components/layout/Layout.tsx
git commit -s -m "feat(i18n): add zh/en toggle in sidebar footer"
```

---

## Task 7: 框架层迁移（`router.tsx` + `Layout.tsx` + `ConnectionBanner.tsx` + `common/*`）

**Files:**
- Modify: `src/router.tsx`
- Modify: `src/components/layout/Layout.tsx`
- Modify: `src/components/layout/ConnectionBanner.tsx`
- Modify: `src/components/common/ErrorBoundary.tsx`
- Modify: `src/i18n/locales/zh.ts`, `src/i18n/locales/en.ts`（已含所需 namespace）

- [ ] **Step 1: 迁移 `router.tsx` 的 `PageLoader`**

`PageLoader` 内 `"Loading…"` → `t("router.loading")`。由于 `PageLoader` 是普通函数组件，顶部加 `import { useI18n } from "@/i18n";`，函数体内 `const { t } = useI18n();`，渲染：
```tsx
function PageLoader() {
  const { t } = useI18n();
  return (
    <div className="flex h-[60vh] items-center justify-center text-muted-foreground">
      {t("router.loading")}
    </div>
  );
}
```

- [ ] **Step 2: 迁移 `Layout.tsx` 全部可见文案**

应用「迁移通用方法」。具体替换映射（`Layout.tsx` 已解构 `t`）：

| 位置 | 原文 | 替换为 |
| --- | --- | --- |
| `NAV` 数组 label | `"Home"/"Agent"/"Alpha Zoo"/"Settings"/"Correlation Matrix"` | 改字段为 `labelKey: "nav.home"/...`（示例 A），渲染处 `!collapsed && t(item.labelKey)` |
| sessions 区标题 | `Sessions` | `t("layout.sessions")` |
| New Chat 链接 title | `"New Chat"` | `t("layout.newChat")` |
| 空会话 | `No sessions yet` | `t("layout.noSessions")` |
| 删除确认 | `Confirm` / `Cancel` | `t("common.confirm")` / `t("common.cancel")` |
| hover 按钮 title | `"Rename"` / `"Delete?"` | `t("common.rename")` / `t("common.delete")` |
| footer dark 按钮 | `Light`/`Dark` 文字与 `title` | `t("layout.light")`/`t("layout.dark")` |
| collapse 按钮 title | `"Expand"`/`"Collapse"` | `t("common.expand")`/`t("common.collapse")` |
| 品牌 | `"Vibe-Trading-Desktop"` | 保留原文（产品名不翻译） |

> `NAV` 的类型从 `{...; label: string}` 变为 `{...; labelKey: NestedKeyOf}` —— 用 `as const` 让 `labelKey` 成为字面量，确保 `t()` 接受。若 `tsc` 报 `labelKey` 类型不兼容，在 `NAV` 项上加 `labelKey: "nav.home" as const`。

- [ ] **Step 3: 迁移 `ConnectionBanner.tsx`**

读取文件，把 `Connection lost` → `t("connection.lost")`；若文件中还有「重连/重试次数」等可见文案，按语义补 `connection.*` key 到 zh.ts/en.ts 再替换。文件顶部加 `import { useI18n } from "@/i18n";` 并 `const { t } = useI18n();`。

- [ ] **Step 4: 迁移 `common/ErrorBoundary.tsx`**

`"Something went wrong"` → `t("errorBoundary.title")`。注意：`ErrorBoundary` 是**类组件**，不能用 hook。两种处理：
- 方案 1（推荐）：把该字符串保留为 prop/默认值，由一个内部函数组件渲染错误 UI，该子组件用 `useI18n()`。
- 方案 2：用模块级 `i18n` 模块导出一个非 hook 的 `tFor(lang)`——但本架构 `t` 绑定 Context，需 hook。

**采用方案 1**：将 `ErrorBoundary.render` 中渲染错误信息的那段 JSX 抽成一个内部函数组件 `ErrorFallback`，在 `ErrorFallback` 内 `const { t } = useI18n();` 并用 `t("errorBoundary.title")`；若现有有「Reload」按钮，文字用 `t("errorBoundary.reload")`。`ErrorBoundary` 本体（类）保持，只把"显示文案的部分"交给 `ErrorFallback`。

> 若 ErrorBoundary 当前结构使抽出组件改动过大，最小可行做法：`ErrorBoundary` 包一层 `<LanguageProvider>` 已存在（全局），可在 `ErrorFallback` 直接用 hook。确认 `ErrorFallback` 渲染在 Provider 内即可（是的，Provider 在 `main.tsx` 最外层）。

- [ ] **Step 5: 构建 + 单测**

Run: `cd frontend && npm run build && npx vitest run`
Expected: PASS（构建无类型错误；现有 `components/__tests__` 全绿）。

- [ ] **Step 6: (可选) Commit**

```bash
git add -A && git commit -s -m "feat(i18n): migrate framework layer (router/layout/connection/errorBoundary)"
```

---

## Task 8: chat 层迁移

**Files:**
- Modify: `src/components/chat/*.tsx`（13 个组件）
- Modify: `src/i18n/locales/zh.ts`, `en.ts`（追加 `chat` namespace）

- [ ] **Step 1: `chat` namespace 命名规范（实际字典随 Step 2 首个文件迁移时创建并填完整值）**

`chat` namespace 在 `errorBoundary` 之后创建。**每条 key 必须在填入完整中英文值后才替换对应代码**——不要先写空值（空值会触发 `locales.test.ts` 的 no-empty-values 用例失败）。命名规范：

```
chat.welcome.{greeting, subtitle, sampleDesc, groupBacktest, groupResearch, groupSwarm, groupDoc, sample1Title, sample1Desc, sample1Prompt, ...}
chat.message.{copy, copied}            // MessageBubble
chat.runner.{...}                       // RunnerStatus
chat.mandate.{...}                      // MandateProposalCard
chat.runComplete.{...}                  // RunCompleteCard
chat.swarm.{...}                        // SwarmStatusCard
chat.toolProgress.{...}                 // ToolProgressIndicator
chat.thinking.{...}                     // ThinkingTimeline
chat.metrics.{...}                      // MetricsCard（指标名复用 charts.metrics.*）
chat.pine.{...}                         // PineScriptViewer
```

种子条目（首条即可写入，含完整双语，作为 `chat` namespace 的开端）：

zh.ts：
```ts
  chat: {
    welcome: {
      sampleDesc: "解析 {{sources}} CSV — 持有天数、胜率、盈亏比、小时分布",
    },
  },
```
en.ts（对应）：
```ts
  chat: {
    welcome: {
      sampleDesc: "Parse {{sources}} CSV — holding days, win rate, PnL ratio, hourly distribution",
    },
  },
```

> Agent 页输入框 `placeholder` 属 `pages.agent.input.placeholder`（Task 10），不放在 `chat`。

- [ ] **Step 2: 逐组件应用「迁移通用方法」**

按以下顺序处理 13 个文件，每个文件：读取 → 为每条可见英文命名 key 填入 zh.ts/en.ts（中文译文 + 英文原值）→ 替换为 `t("chat.<sub>.<key>")`：

1. `WelcomeScreen.tsx`：问候/副标题、4 个分组 `label`、各示例 `title`/`desc`；`sampleDesc` 用插值（示例 C，`sources` 传 `"同花顺/东财/富途"`，英文版传 `"generic"` 或保留多源串）。示例 `prompt` 字段（发给 agent 的指令）：**中文界面翻译为中文 prompt、英文界面保留英文 prompt**——用 `t("chat.welcome.sample1Prompt")` 等 key 分别维护。
2. `MessageBubble.tsx`：复制按钮 `title="Copy"` → `t("chat.message.copy")`，复制成功提示 → `t("chat.message.copied")`；角色标签等。
3. `RunnerStatus.tsx`：状态文本。
4. `MandateProposalCard.tsx`：提案相关按钮/标签。
5. `RunCompleteCard.tsx`：完成卡片标题/按钮。
6. `SwarmStatusCard.tsx`：swarm 状态文案。
7. `ToolProgressIndicator.tsx`：工具进度文案。
8. `ThinkingTimeline.tsx`：思考过程文案。
9. `MetricsCard.tsx`：指标卡标签（指标名本身复用 `charts.metrics.*`）。
10. `PineScriptViewer.tsx`：查看器标题/按钮。
11. `ConversationTimeline.tsx`：时间线文案（若有）。
12. `AgentAvatar.tsx`：通常无文案，跳过（确认无可见字符串即可）。
13. `ProgressBar.tsx`：通常无文案，跳过。

> 每个文件改完跑一次 `npm run build` 逮类型/key 错误。所有文件共用 `useI18n()`，顶部 `import { useI18n } from "@/i18n";` + `const { t } = useI18n();`。

- [ ] **Step 3: 构建 + 单测**

Run: `cd frontend && npm run build && npx vitest run`
Expected: PASS。

- [ ] **Step 4: (可选) Commit**

```bash
git add -A && git commit -s -m "feat(i18n): migrate chat layer components"
```

---

## Task 9: charts 层迁移（含 `METRIC_LABELS`）

**Files:**
- Modify: `src/lib/formatters.ts`
- Modify: `src/components/charts/*.tsx`（5 个）
- Modify: `src/i18n/locales/zh.ts`, `en.ts`（追加 `charts` namespace）

- [ ] **Step 1: 在 zh.ts/en.ts 追加 `charts.metrics` 与 `charts.validation`**

`charts.metrics`（迁自 `formatters.ts` 的 `METRIC_LABELS`）：

```ts
  charts: {
    metrics: {
      total_return: "总收益",
      annual_return: "年化",
      sharpe: "夏普",
      max_drawdown: "最大回撤",
      win_rate: "胜率",
      trade_count: "交易数",
      final_value: "最终价值",
      calmar: "卡玛",
      sortino: "索提诺",
      profit_loss_ratio: "盈亏比",
      max_consecutive_loss: "最大连亏",
      avg_holding_days: "平均持仓天数",
      benchmark_return: "基准收益",
      excess_return: "超额收益",
      information_ratio: "信息比率",
    },
    validation: {
      monteCarloTitle: "蒙特卡洛置换检验",
      significant: "显著",
      notSignificant: "不显著",
      actualSharpe: "实际夏普",
      simulatedMean: "模拟均值",
      observedSharpe: "观察夏普",
      medianSharpe: "中位夏普",
      bootstrapTitle: "自助法夏普置信区间",
      walkForwardTitle: "滚动前瞻分析",
      consistency: "一致性",
      avgReturn: "平均收益",
      avgSharpe: "平均夏普",
      windows: "窗口数",
      period: "周期",
      return: "收益",
      sharpe: "夏普",
      maxDd: "最大回撤",
      trades: "交易数",
      winRate: "胜率",
      noData: "暂无验证数据。",
    },
  },
```

`en.ts` 对应 `charts`：`metrics` 值用原 `METRIC_LABELS` 英文（Total Return / Annual / Sharpe / Max DD / Win Rate / Trades / Final Value / Calmar / Sortino / P/L Ratio / Max Consec. Loss / Avg Hold Days / Benchmark / Excess Return / IR）；`validation` 值用原英文（Monte Carlo Permutation Test / Significant / Not Significant / Actual Sharpe / Simulated Mean / Observed Sharpe / Median Sharpe / Bootstrap Sharpe CI / Walk-Forward Analysis / Consistency / Avg Return / Avg Sharpe / Windows / Period / Return / Sharpe / Max DD / Trades / Win Rate / No validation data available.）。

- [ ] **Step 2: 改造 `formatters.ts` 的 `getMetricLabel`**

将硬编码 `METRIC_LABELS` 改为接受 `t` 函数。新增导出（保留旧函数签名以减小改动面）：

```ts
// 顶部删除原 METRIC_LABELS 常量；改为：
import type { Vars } from "@/i18n/types";

const METRIC_KEY_MAP: Record<string, string> = {
  total_return: "charts.metrics.total_return",
  annual_return: "charts.metrics.annual_return",
  sharpe: "charts.metrics.sharpe",
  max_drawdown: "charts.metrics.max_drawdown",
  win_rate: "charts.metrics.win_rate",
  trade_count: "charts.metrics.trade_count",
  final_value: "charts.metrics.final_value",
  calmar: "charts.metrics.calmar",
  sortino: "charts.metrics.sortino",
  profit_loss_ratio: "charts.metrics.profit_loss_ratio",
  max_consecutive_loss: "charts.metrics.max_consecutive_loss",
  avg_holding_days: "charts.metrics.avg_holding_days",
  benchmark_return: "charts.metrics.benchmark_return",
  excess_return: "charts.metrics.excess_return",
  information_ratio: "charts.metrics.information_ratio",
};

/** Resolve a metric key to its translated label via the provided loose translator (`tRaw`). */
export function getMetricLabel(
  k: string,
  resolve: (path: string, vars?: Vars) => string,
): string {
  const path = METRIC_KEY_MAP[k];
  return path ? resolve(path) : k;
}
```

> `metricSentiment` / `formatMetricVal` / `DISPLAY_ORDER` 等保持不变（它们不涉及可见文案，只做数值/逻辑判断）。

- [ ] **Step 3: 更新 `getMetricLabel` 的所有调用点**

搜索调用：`cd frontend && grep -rn "getMetricLabel" src/`。每个调用点现已需要传 `tRaw`。调用组件内：`const { tRaw } = useI18n(); ... getMetricLabel(k, tRaw)`。逐处更新。

- [ ] **Step 4: 迁移 5 个 charts 组件可见文案**

应用「迁移通用方法」：
1. `ValidationPanel.tsx`：3 个 `<h4>` 标题 → `t("charts.validation.monteCarloTitle"/"bootstrapTitle"/"walkForwardTitle")`；`Badge value` 的 `"Significant"/"Not Significant"` → `t(...)`；各 `Stat label="..."` → `t(...)`（示例 D）；表头 `Period/Return/Sharpe/Max DD/Trades/Win Rate` → `t(...)`；空状态 `No validation data available.` → `t("charts.validation.noData")`（示例 E）。
2. `EquityChart.tsx`：标题/图例文案（若有）。
3. `CandlestickChart.tsx`：同上。
4. `CorrelationMatrix.tsx`：标题/提示（若有）。
5. `MiniEquityChart.tsx`：通常无文案，确认即可。

> ECharts 内的坐标轴 `name`/`tooltip` 文案也属可见文案，一并迁移；纯技术配置（`type:"category"` 等）不动。

- [ ] **Step 5: 更新 `formatters` 相关单测**

`src/lib/__tests__/formatters.test.ts` 中 `getMetricLabel` 用例需改为传入一个 mock `t`：
```ts
const resolve = (p: string) => p; // identity for test
it("returns human label for known keys", () => {
  expect(getMetricLabel("sharpe", resolve)).toBe("charts.metrics.sharpe");
});
it("returns raw key for unknown keys", () => {
  expect(getMetricLabel("custom_metric", resolve)).toBe("custom_metric");
});
```
删除对旧 `METRIC_LABELS` 的导入断言（若测试引用了它）。

- [ ] **Step 6: 构建 + 单测**

Run: `cd frontend && npm run build && npx vitest run`
Expected: PASS。

- [ ] **Step 7: (可选) Commit**

```bash
git add -A && git commit -s -m "feat(i18n): migrate charts layer + METRIC_LABELS to t()"
```

---

## Task 10: pages 迁移 — `Home.tsx` + `Agent.tsx`

**Files:**
- Modify: `src/pages/Home.tsx`, `src/pages/Agent.tsx`
- Modify: `src/i18n/locales/zh.ts`, `en.ts`（追加 `pages.home`, `pages.agent`）

- [ ] **Step 1: `pages` namespace 命名规范（实际字典随 Step 2/3 迁移时创建并填完整值）**

`pages` namespace 在此 Task 首次创建（此前不存在）。**每条 key 填入完整中英文值后才替换代码**（不写空值，否则 `locales.test.ts` no-empty-values 失败）。命名规范：

```
pages.home.{title, ...}                 // Home.tsx
pages.agent.{input.placeholder, ...}    // Agent.tsx（input.placeholder 英文用现有输入框原文）
```

> `settings`/`compare`/`correlation`/`alphaZoo`/`runDetail` 子 namespace 由 Task 11–14 各自追加，本 Task 不预先创建。

- [ ] **Step 2: 迁移 `Home.tsx`**

读取文件，把所有可见英文（页面标题、卡片标题/描述、按钮、空状态、说明文字）按「迁移通用方法」迁到 `pages.home.*`。顶部 `import { useI18n } from "@/i18n";` + `const { t } = useI18n();`。

- [ ] **Step 3: 迁移 `Agent.tsx`**

读取文件，迁移可见文案到 `pages.agent.*`；输入框 `placeholder` → `t("pages.agent.input.placeholder")`。注意：agent **消息列表内容**（来自后端）不迁移，只迁移 chrome（输入框、按钮、标题、空状态、发送/停止按钮 label 等）。

- [ ] **Step 4: 构建 + 单测**

Run: `cd frontend && npm run build && npx vitest run`
Expected: PASS。

- [ ] **Step 5: (可选) Commit**

```bash
git add -A && git commit -s -m "feat(i18n): migrate Home + Agent pages"
```

---

## Task 11: pages 迁移 — `Settings.tsx`

**Files:**
- Modify: `src/pages/Settings.tsx`
- Modify: `src/i18n/locales/zh.ts`, `en.ts`（填齐 `pages.settings`）

- [ ] **Step 1: 读取 `Settings.tsx`，枚举可见文案**

Settings 页含表单（LLM 提供商、数据源等配置）——有大量 label/section title/按钮/说明/占位符/状态提示。

- [ ] **Step 2: 填齐 `pages.settings` namespace（zh + en）并迁移**

按「迁移通用方法」命名（如 `llmSectionTitle`/`dataSourceSectionTitle`/`saveBtn`/`testBtn`/`savedToast`/`providerLabel`/`apiKeyPlaceholder` 等），逐条加字典再替换。**表单字段的技术 key（如 `"openrouter"`/`"tushare"` 等选项 value）不翻译**，只翻译展示给用户的 label/description。

- [ ] **Step 3: 构建 + 单测**

Run: `cd frontend && npm run build && npx vitest run`
Expected: PASS。

- [ ] **Step 4: (可选) Commit**

```bash
git add -A && git commit -s -m "feat(i18n): migrate Settings page"
```

---

## Task 12: pages 迁移 — `Compare.tsx` + `Correlation.tsx`

**Files:**
- Modify: `src/pages/Compare.tsx`, `src/pages/Correlation.tsx`
- Modify: `src/i18n/locales/zh.ts`, `en.ts`（填齐 `pages.compare`, `pages.correlation`）

- [ ] **Step 1: 迁移 `Compare.tsx`**

读取 → 填齐 `pages.compare.*`（标题、运行选择器 label、对比维度标签、空状态、按钮、表格表头）→ 替换为 `t()`。指标名复用 `charts.metrics.*`（经 `getMetricLabel(k, t)`）。

- [ ] **Step 2: 迁移 `Correlation.tsx`**

读取 → 填齐 `pages.correlation.*`（标题、资产选择、说明、空状态、图例）→ 替换。`CorrelationMatrix` 组件文案已在 Task 9 处理，本页只处理页面级文案。

- [ ] **Step 3: 构建 + 单测**

Run: `cd frontend && npm run build && npx vitest run`
Expected: PASS。

- [ ] **Step 4: (可选) Commit**

```bash
git add -A && git commit -s -m "feat(i18n): migrate Compare + Correlation pages"
```

---

## Task 13: pages 迁移 — `AlphaZoo.tsx`

**Files:**
- Modify: `src/pages/AlphaZoo.tsx`
- Modify: `src/i18n/locales/zh.ts`, `en.ts`（填齐 `pages.alphaZoo`）

- [ ] **Step 1: 迁移 `AlphaZoo.tsx`**

AlphaZoo 含多种子视图（list/bench/compare/detail）。读取 → 填齐 `pages.alphaZoo.*`（页面标题、tab 标签、列名、空状态、按钮、说明、状态提示）→ 替换。Alpha 标识符（alpha id）不翻译。

- [ ] **Step 2: 构建 + 单测**

Run: `cd frontend && npm run build && npx vitest run`
Expected: PASS。

- [ ] **Step 3: (可选) Commit**

```bash
git add -A && git commit -s -m "feat(i18n): migrate AlphaZoo page"
```

---

## Task 14: pages 迁移 — `RunDetail.tsx`

**Files:**
- Modify: `src/pages/RunDetail.tsx`
- Modify: `src/i18n/locales/zh.ts`, `en.ts`（填齐 `pages.runDetail`）

- [ ] **Step 1: 迁移 `RunDetail.tsx`**

读取 → 填齐 `pages.runDetail.*`（标题、tab、代码/Pine 视图标签、运行状态、按钮、空/错误状态）→ 替换。代码内容、运行日志不翻译。

- [ ] **Step 2: 构建 + 单测**

Run: `cd frontend && npm run build && npx vitest run`
Expected: PASS。

- [ ] **Step 3: (可选) Commit**

```bash
git add -A && git commit -s -m "feat(i18n): migrate RunDetail page"
```

---

## Task 15: 全量回归与验收

**Files:** 无新增/修改（验证性任务）

- [ ] **Step 1: 字典一致性回归**

Run: `cd frontend && npx vitest run src/i18n/__tests__/locales.test.ts`
Expected: PASS（zh/en key 完全一致、无非空值）。

- [ ] **Step 2: 全量构建 + 全量单测**

Run: `cd frontend && npm run build && npx vitest run`
Expected: 全绿（含新增 i18n 三组测试 + 现有所有测试）。

- [ ] **Step 3: 残留硬编码扫描**

Run: `cd frontend && grep -rnE '>[A-Z][a-z]+ ?[a-zA-Z]*<|placeholder="[A-Z][a-zA-Z ]+"|title="[A-Z][a-zA-Z ]+"' src/pages src/components --include=*.tsx | grep -vE 'className|import|from |=>|key=|t\(|\{t\(|stroke|fill|d="M|viewBox|xmlns|http|\.com|@[a-z]' | head -40`
Expected: 输出应为空，或仅剩**刻意保留**的（产品名、专有名词、动态内容、注释）。逐条核对剩余项，确认是刻意保留则放行，否则补迁移。

- [ ] **Step 4: 手动运行验证（`npm run dev`）**

Run: `cd frontend && npm run dev`（另需后端 `vibe-trading serve` 或仅看 UI chrome）
在浏览器 `http://localhost:5899`：
- 首次访问界面应为**简体中文**。
- 点 sidebar footer 切换器 → 界面即时切英文，`<html lang="en">`。
- 刷新 → 保持英文；再切回中文 → `<html lang="zh">`，刷新保持。
- 巡查每个路由（`/`、`/agent`、`/settings`、`/compare`、`/correlation`、`/alpha-zoo`、`/runs/:id`）两种语言下无英文残留、无 `xxx.yyy` 裸 key 显示、布局无错位。

- [ ] **Step 5: 对照 spec 验收清单确认**

逐项核对 `docs/superpowers/specs/2026-06-13-frontend-i18n-design.md` §7 验收标准，全部勾选。

- [ ] **Step 6: (可选) 最终 Commit**

```bash
git add -A && git commit -s -m "test(i18n): full regression — zh/en parity, no hardcoded UI strings"
```

---

## 完成定义（Definition of Done）

- `src/i18n/`（types/translator/locales/context/index）就位，三组单测全绿。
- `main.tsx` 挂载 `LanguageProvider`，sidebar footer 有可用的中/英切换器。
- `router` + 全部 `pages`(7) + `components/{chat,charts,common,layout}` 的可见静态文案均已 `t()` 化。
- `npm run build` 与 `npx vitest run` 全绿；硬编码扫描无意外残留。
- 默认中文、切换即时生效、localStorage 持久化、`<html lang>` 同步，符合 spec §7。
