{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the main window and all internal app webviews (app + shell + grid). External tab-* webviews are deny-by-default.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:event:default",
    "process:default"
  ]
}
