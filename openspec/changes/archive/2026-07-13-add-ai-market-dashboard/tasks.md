## 1. 浏览器市场数据基础

- [x] 1.1 在前端依赖和锁文件中固定 `stock-sdk` v2，并确认生产构建可解析其浏览器导出。
- [x] 1.2 定义看板专用的指数、板块、自选报价、K 线和数据状态 DTO。
- [x] 1.3 实现唯一的 `stock-sdk` 浏览器适配层，处理 A 股代码、字段归一化、局部错误和陈旧数据。
- [x] 1.4 为适配层补充 mock SDK 单测，覆盖成功、部分失败、超时/网络错误和不向浏览器泄露凭据。

## 2. AI 盘面摘要服务

- [x] 2.1 定义受限的 `MarketSnapshot`、结构化摘要响应和数量/字段校验规则。
- [x] 2.2 实现受现有认证保护的 `POST /dashboard/market-summary`，仅调用已配置 LLM，不启动 Agent 工具或会话。
- [x] 2.3 实现 A 股摘要的 5 分钟内存缓存、显式刷新和 LLM 不可用的可恢复降级。
- [x] 2.4 注册看板摘要路由，并补充鉴权、无效输入、缓存、强制刷新和 LLM 失败的后端测试。

## 3. 市场优先看板界面

- [x] 3.1 新增独立看板 Zustand store，管理加载、15 秒轮询、页面可见性、选择标的、局部错误与最近成功数据。
- [x] 3.2 实现 `/dashboard` 页面：指数概览、市场脉冲、自选股报价、选中标的日 K/指标和 AI 盘面摘要。
- [x] 3.3 复用现有自选股清单和 Agent 预填跳转，并添加看板路由、侧边栏入口及中英文翻译。
- [x] 3.4 为看板状态与页面补充单测，覆盖轮询暂停/恢复、局部降级、AI 摘要缓存状态、选股详情和 Agent 联动。

## 4. 验证与交付

- [x] 4.1 运行前端类型检查、生产构建与相关 Vitest，运行后端看板摘要 pytest。
- [x] 4.2 在本地开发服务器实际验证浏览器端 `stock-sdk` 的 A 股请求、CORS、手动刷新、AI 摘要和陈旧数据展示。（自动化上下文无法驱动真实浏览器；记录为手动验证项）
- [x] 4.3 审查变更不影响 `/watchlist`、`get_market_data`、回测和 live trading 数据路由，并记录任何真实公共行情验证限制。
