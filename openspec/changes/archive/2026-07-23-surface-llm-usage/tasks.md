## 1. Regression Tests

- [x] 1.1 扩展 AgentLoop 用量测试，覆盖供应商报告的缓存读取/写入字段、累积、原子产物内容，以及不存在凭据或对话内容。
- [x] 1.2 扩展运行详情 API 测试，覆盖增强后的用量摘要和仅包含既有 totals 的历史 `llm_usage.json` 文件。
- [x] 1.3 添加前端单元测试，覆盖实时 `llm_usage` 事件、持久化运行用量、缓存未提供，以及未来可计量 VIP 用量与用户自带 Key 用量的区分。

## 2. Backend Usage Contract

- [x] 2.1 扩展 AgentLoop 用量规范化和聚合，保留已识别的供应商报告缓存读取/写入 Token 值，不估算缺失值。
- [x] 2.2 添加向后兼容的 `metering_eligible` 分类，仅标识平台托管 `vip_server` 用量，且不暴露凭据。
- [x] 2.3 确保持久化用量产物、SSE 增量和运行详情 API 一致暴露可选的缓存与计量字段，同时保留既有 totals 契约。

## 3. WebUI Data Integration

- [x] 3.1 为运行级和增量 LLM 用量数据添加向后兼容的 TypeScript 类型，包括可选缓存和计量字段。
- [x] 3.2 在 Agent 运行状态中消费 `llm_usage` SSE 事件，并从既有运行详情响应填充已完成或恢复的运行。

## 4. Usage Presentation

- [x] 4.1 在 Agent 工作流中添加无障碍、已国际化的 LLM 用量视图，展示 Provider、模型、调用次数、输入、输出、总量以及仅由供应商报告的缓存指标。
- [x] 4.2 清晰呈现空状态、缓存未提供、历史运行和未来计量资格状态，不展示余额、充值流程、重置计划、账单金额或额度拦截。

## 5. Verification

- [x] 5.1 运行定向后端与前端测试套件、前端生产构建以及受影响模块的静态检查。
- [x] 5.2 手动验证实时运行和历史运行显示相同的累计用量，并确认未呈现 API Key、登录 token、提示词、响应或账户标识。

<!-- 2026-07-22: 用户授权跳过真实 Provider UI 对照；缺少可安全调用的无凭据测试端点。自动化与静态替代证据记录在既有验证报告。 -->

## 6. Global Usage Aggregation

- [x] 6.1 以 TDD 扩展 SessionStore 和后端聚合服务，按 attempt 与历史消息关联/去重运行，覆盖时间范围、IANA 时区、总览、趋势、Provider/模型分布、缓存覆盖率及缺失/损坏文件降级。
- [x] 6.2 新增受认证保护的 `GET /usage/llm` 契约与 API 测试，支持会话搜索和分页，确保搜索/分页不改变完整时间范围的总览统计。

## 7. Global Usage Center

- [x] 7.1 以 TDD 新增 `/usage` 懒加载页面、侧栏一级入口、聚合 API 类型和国际化界面，实现全部历史/日期筛选、手动刷新、总览、趋势、Provider/模型分布及可展开会话运行明细。
- [x] 7.2 从 Agent 实时区域和历史消息中移除单次用量卡片及其页面状态，保留 Run Detail 运行级用量，并更新相关回归测试。

## 8. Revised Verification

- [x] 8.1 运行新增后端与前端定向测试、locale 完整性测试和前端生产构建，并验证聚合端点不暴露凭据、消息正文、运行 prompt 或模型响应。
