# Frontend 环境变量示例
# 用法：cp frontend/.env.example frontend/.env.local  然后按需取消注释填值
# 所有变量均为可选 —— 未设置时代码使用下列默认值，开箱即用。

# === Vibe Trading 主后端（agent / FastAPI）===
# Vite dev server 将 /sessions、/runs、/mandate 等 PROXY_PATHS 转发到这里。
# VITE_API_URL=http://127.0.0.1:8899

# === VIP 用户服务后端（cool-admin）===
# 登录 / 注册 / 验证码 / 用户信息 走这里。
#  - Vite dev server 将 /user-api 前缀转发到这里（前缀被 rewrite 剥掉）
#  - Login.tsx 用它向 maas 端点自动配置 LLM 设置
# VITE_USER_API_URL=http://127.0.0.1:8001

# === apiUser 请求路径前缀 ===
# 仅在需要把 /user-api 改成别的路径时才覆盖（一般无需改动）。
# VITE_USER_API_BASE=/user-api
